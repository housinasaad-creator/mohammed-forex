services:
  - type: web
    name: forex-bridge
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn bridge:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
